# Closed-Line Index For Clean Full `simple support / подвижный шарнир`

This file is a short freeze/archive index for the old clean full
`simple support / подвижный шарнир` theorem-facing line.

It does not reopen that line, does not introduce a new theorem step, and does
not claim that the current critical-load criterion is proved or refuted.

It is archive/frozen-line navigation only. It is not the primary current
working-order note for the clean full `simple support / подвижный шарнир`
criterion-rebuild branch.

For the current criterion-facing working order on that branch, use
`docs/theory/current_simple_support_criterion_rebuild_note.md`.

Stable object definitions used across the frozen-line documents are centralized
in `docs/theory/current_simple_support_object_glossary.md`.

## 1. Original Goal

The old line tried to determine whether the current theorem-facing criterion
could be justified strongly enough to exclude the explicit membrane channel on
the clean full `simple support / подвижный шарнир` path, and hence support the
criterion-level reading used for critical-load / wave-number selection.

## 2. What Was Actually Established

- the old theorem-facing line reached a genuine saturation point on the current
  clean repository boundary;
- the explicit weighted-ansatz membrane candidate remained alive enough to keep
  membrane-channel skepticism active on that boundary;
- the current criterion still did not gain theorem-facing authority to exclude
  that explicit membrane candidate;
- the admissibility-side question was sharpened from a broad extension problem
  to one explicit same-trace fixed-fiber question, then to one explicit affine-
  line question, and finally to one residual-direction question for `z_temp`;
- the selected-family / trace / fixed-fiber / fiber-excess identities produced
  by the old line remain reusable repository-level structure.

## 3. What Was Not Proved Or Refuted

- it was **not** proved that the current criterion is theorem-secured;
- it was **not** proved that the current criterion fails;
- it was **not** proved that the explicit membrane candidate is theorem-facing
  admissible;
- it was **not** proved that the explicit affine line collapses to `c_sel`
  inside theorem-facing admissibility;
- it was **not** proved that a nonzero admissible point on that explicit line
  exists;
- it did **not** settle final physical critical load / wave number.

## 4. Exact Final Unresolved Question

The final sharpened admissibility-side boundary of the old line is:

```text
z_temp,n(q;s_mem) in A_adm^th,n(q) intersect ker(C_center,n(q)) ?
```

Equivalently:

```text
(c_sel + span(z_temp)) intersect A_adm^th,n(q) = {c_sel} ?
```

So the old line ends at the question whether theorem-facing admissibility
contains the explicit residual direction `z_temp`, or collapses on that line to
the selected representative `c_sel` alone.

## 5. Why The Line Is Frozen

The line is frozen because the last honest sharpening already reached the
residual-direction boundary above, and no genuinely new continuum /
equation-level admissibility input was produced on the current clean boundary.

Further continuation in the same old style would therefore likely be
reformulation only.

## 6. Reusable Results For Criterion Reformulation

If the critical-load criterion is reformulated later, the following outputs of
the frozen line should be reused:

- the criterion still lacks theorem-facing authority to exclude the explicit
  membrane candidate on the current clean boundary;
- the old line produced reusable negative knowledge, even though it did not
  validate or refute the criterion;
- the current selected-family interpretation
  `A_ls = im(V_adm) = im(M_amp)` remains the right repo-level selected-family
  reading on the weighted-ansatz boundary;
- the selected trace object `J_0(A_ls) = im(D_amp)` remains reusable;
- the fixed same-trace fiber and fiber-excess identities remain reusable as the
  correct repository-level geometric scaffold around the explicit membrane
  candidate;
- any future theorem return on this topic now requires genuinely new continuum
  / equation-level admissibility input.

## 7. Archive Reading Order

For the live clean criterion-facing working order, start from
`docs/theory/current_simple_support_criterion_rebuild_note.md`, not from this
archive index.

When you specifically need the frozen-line archive, read in this order:

1. `docs/theory/current_simple_support_final_audit_note.md`
2. `docs/theory/current_simple_support_object_glossary.md`
3. `proof_pilots/pilot_23_clean_simple_support_reduced_tangent_operator/pilot_23_clean_simple_support_reduced_tangent_operator.md`
4. `proof_pilots/pilot_24_t3a_selected_kernel_bridge/pilot_24_t3a_selected_kernel_bridge.md`
5. `proof_pilots/pilot_25_t3b_selected_class_upgrade/pilot_25_t3b_selected_class_upgrade.md`
6. `docs/theory/current_simple_support_theorem_roadmap.md`
7. `docs/theory/current_theory_verification_map.md`
8. `docs/journal/project_journal_updated14.md`
9. `docs/assumptions/assumptions.md`
10. `docs/theory/vyvod_uravneniy_updated17.md`
